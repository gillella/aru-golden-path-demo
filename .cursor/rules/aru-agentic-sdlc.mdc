---
description: Aru minimal governance for this repository
alwaysApply: true
---

Read `AGENTS.md`, `.aru/verify.sh`, and
`$ARU_SDLC_HOME/docs/KERNEL-CONTRACT.md` before governed software work.
The repository's AGENTS file defines the current six skills, Ready contract,
exclusive claims, isolated worktrees, exact-head self-hosted verification,
risk-tiered independent review, and helper-controlled merge.

Use `fetch_next_work.py` to select one bounded task. GitHub Issues and the
linked Project Board are lifecycle authority; free-form requests do not
create a persistent loop or select a retired skill.
